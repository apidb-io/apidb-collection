These are the ansible variables from the playbook:
you can use them in a template like this:

environment is {{ server_env }}
The server support team is the {{ support_team }} team.
Your EC2 regions is {{ region }}.
